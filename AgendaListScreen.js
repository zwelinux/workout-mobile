import React from 'react';
import { View, Text, Image, StyleSheet, TouchableOpacity, ScrollView, ImageBackground } from 'react-native';

function AgendaListScreen() {
    return (        
    
    <View>
        <Text>
          Start Now Screen
        </Text>
    </View>
    
)}

export default AgendaListScreen